name: Screenshots

on:
  workflow_dispatch:
  push:
    branches: [main]
    paths:
      - 'Interclip/**'
      - 'InterclipShared/**'
      - 'InterclipUITests/**'
      - 'fastlane/Snapfile'

jobs:
  screenshots:
    runs-on: macos-15

    steps:
      - uses: actions/checkout@v4

      - uses: ruby/setup-ruby@v1
        with:
          ruby-version: '3.3'
          bundler-cache: true

      - name: Select Xcode
        run: |
          # Pick the latest Xcode available on this runner
          XCODE=$(ls /Applications/ | grep '^Xcode' | grep '\.app$' | sort -V | tail -1)
          echo "Using $XCODE"
          sudo xcode-select -s "/Applications/$XCODE"
          xcodebuild -version

      - name: Detect iOS runtime version
        run: |
          IOS=$(xcrun simctl list runtimes | grep -E "^iOS " | grep -v unavailable | sort -V | tail -1 | grep -oE '[0-9]+\.[0-9]+' | head -1)
          echo "IOS_VERSION=$IOS" >> $GITHUB_ENV
          echo "Detected iOS $IOS"

      - name: Capture screenshots
        run: bundle exec fastlane snapshot

      - uses: actions/upload-artifact@v4
        if: always()
        with:
          name: screenshots
          path: fastlane/screenshots
          retention-days: 30
